name: Deploy Web to S3 + CloudFront

on:
  push:
    branches:
      - stfc-dev # stfc-dev 브랜치 푸시 시 실행
    paths:
      - 'apps/web/**'
      - '.github/workflows/deploy-web.yml'
  workflow_dispatch:  # 수동 실행 가능

# OIDC 사용을 위한 권한 설정
permissions:
  id-token: write
  contents: read

jobs:
  deploy-web:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build Web (Static Export)
        run: npm run build:web
        env:
          NODE_ENV: production
      
      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ap-southeast-1
      
      - name: Upload to S3 Singapore
        run: |
          aws s3 sync apps/web/out/ s3://plcr-web-singapore/ --delete
      
      - name: Invalidate CloudFront Cache
        run: |
          aws cloudfront create-invalidation \
            --distribution-id E2M7ZCNM694ZRS \
            --paths "/*"
      
      - name: Deployment Summary
        run: |
          echo "✅ Deployment completed!"
          echo "📦 Bucket: s3://plcr-web-singapore/"
          echo "🌐 Region: ap-southeast-1 (Singapore)"
          echo "🔄 CloudFront: Cache invalidated"
          echo "🌍 URL: https://d1577wnsw99fb9.cloudfront.net"
