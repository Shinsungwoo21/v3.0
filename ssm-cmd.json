{
    "commands": [
        "sudo -u ec2-user bash -c 'source /home/ec2-user/.bashrc && source ~/.nvm/nvm.sh && cd ~/megaticket/apps/app && pm2 delete all; pm2 start npm --name app-backend -- start && pm2 save'"
    ]
}